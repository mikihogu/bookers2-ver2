<h4>New book</h4>
<%= form_with model: book do |f| %>
  <div class="form-group">
    <%= f.label :title, "Title" %>
    <%= f.text_field :title, class: "form-control w-100" %>
  </div>
  <div class="form-group">
    <%= f.label :body, "Opinion" %>
    <%= f.text_area :body, class: "form-control w-100", placeholder: "200文字以内" %>
  </div>
  <div class="form-group">
    <%= f.submit "Create Book", class: "btn btn-sm btn-outline-success" %>
  </div>
<% end %>